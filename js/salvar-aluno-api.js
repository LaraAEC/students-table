
 //Como salvar os alunos no backend com o botão salvar?
 /*function salvarAlunoNaApi(aluno) {
    var xmlhttp = new XMLHttpRequest();
    xmlhttp.open("POST", "http://localhost:8080/aluno/salvar"); //Método POST utilizado para enviar/salvar dados na API. E o endpoint pra onde estou enviando, quero o endpoint salvar.  

    xmlhttp.setRequestHeader("Content-Type", "application/x-www-form-urlencoded");

    xmlhttp.onreadystatechange = () => { // Call a function when the state changes.
  if (xmlhttp.readyState === XMLHttpRequest.DONE && xmlhttp.status === 200) {
    // Request finished. Do processing here.
  }
}
    xmlhttp.send(aluno);
// xhr.send(new Int8Array());
// xhr.send(document);
}
  var aluno = obtemAlunoDoFormulario(frmAluno);


  const btnSalvar = document.getElementById('btnSalvarAluno');
  btnSalvar.addEventListener('click', salvarAlunoNaApi(aluno));
*/
